---
"@odysseusdev/hephaestus": patch
---

forge's harness multiselect prompt now defaults to none selected instead of all selected. pick the harness(es) you want instead of deselecting the ones you don't.
