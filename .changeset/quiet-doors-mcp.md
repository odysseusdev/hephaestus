---
"@odysseusdev/hephaestus": patch
---

documented that an agent with no declared tools inherits full harness access instead of none — the sanctioned way to grant capabilities (like MCP) hephaestus has no abstract tool for. added a stderr note when this fires so it reads as intentional, not an oversight. no schema change.
